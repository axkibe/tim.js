#!/bin/bash
if [ ! -f .jshint.stamp ]; then
	touch -d 19700101 .jshint.stamp
fi
( jshint --config jshint.json `find src jioncode -name \*.js -newer .jshint.stamp` && touch .jshint.stamp ) | head -n 10
exit ${PIPESTATUS[0]}
